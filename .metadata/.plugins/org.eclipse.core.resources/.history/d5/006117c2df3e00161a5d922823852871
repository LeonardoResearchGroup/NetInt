package gui;

import java.awt.Color;

import graphElements.Graph;
import processing.core.*;

public class BasicWindow {

	PApplet app;
	PVector pos, end;
	PVector margin;
	Color background, textColor;

	public BasicWindow(PApplet app) {
		this.app = app;
		margin = new PVector(15, 15);
		pos = new PVector(0, 0);
		end = new PVector(app.width, 200);
		background = new Color(10, 0, 0, 80);
		textColor = Color.WHITE;
	}

	public void setBox(int x, int y, int w, int h) {
		pos.set(x, y);
		end.set(w, h);
	}

	public void setMargins(int left, int top) {
		margin.set(left, top);
	}

	public void showStats(Object obj) {
		String txt = null;
		if (obj.getClass().equals(new Graph().getClass())) {
			Graph tmp = (Graph) obj;
			txt = tmp.getBasicStats();
		}else{
			txt = "No text to display";
		}
		
		app.textAlign(PConstants.LEFT, PConstants.TOP);
		app.stroke(Color.WHITE.darker().getRGB(),50);
		app.fill(background.getRGB());
		app.rect(pos.x, pos.y, end.x, end.y);
		app.fill(textColor.darker().getRGB());
		app.text(txt, pos.x + margin.x, pos.y + margin.y);
		app.text("Framerate : "+app.frameRate, 20, 10);
		app.textAlign(PConstants.CENTER, PConstants.CENTER);
	}
}
